# Frontend — apps/frontend/.env (yerel geliştirme)
# Backend API kök adresi (/api dahil). Veriler bu adresten polling ile çekilir.
# Üretimde (Vercel) bu değeri backend Vercel projesinin URL'i yap, örn:
#   https://rebirthpos-api.vercel.app/api
NUXT_PUBLIC_API_BASE="http://localhost:3001/api"
