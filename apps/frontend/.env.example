# Frontend — apps/frontend/.env (yerel geliştirme)
# Backend API kök adresi (/api dahil). WebSocket de bu adresten türetilir.
# Üretimde (Vercel) bu değeri Render backend URL'i yap, örn:
#   https://rebirthpos-backend.onrender.com/api
NUXT_PUBLIC_API_BASE="http://localhost:3001/api"
